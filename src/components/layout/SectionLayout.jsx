function SectionLayout({ children }) {
  return (
    <section className="template min-h-screen flex flex-col items-center justify-center py-20">
      {children}
    </section>
  );
}

export default SectionLayout;
